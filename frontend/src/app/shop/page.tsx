"use client";
import { useMemo, useState } from "react";
import { Search, SlidersHorizontal } from "lucide-react";
import { products } from "@/data/products";
import { ProductCard } from "@/components/ProductCard";

export default function ShopPage() { const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [sort, setSort] = useState("Featured"); const filtered = useMemo(() => { let list = products.filter((p) => (category === "All" || p.category === category) && [p.name, p.brand, p.category, p.sku].some((v) => v.toLowerCase().includes(query.toLowerCase()))); if (sort === "Price low to high") list = [...list].sort((a,b) => a.price-b.price); if (sort === "Price high to low") list = [...list].sort((a,b) => b.price-a.price); if (sort === "Newest") list = list.filter((p) => p.isNew).concat(list.filter((p) => !p.isNew)); return list; }, [category, query, sort]); return <main className="px-5 py-12 lg:px-10 lg:py-20"><div className="mx-auto max-w-[1400px]"><p className="eyebrow">TMTwatch / Shop</p><div className="mt-3 flex flex-col justify-between gap-7 border-b border-black/10 pb-10 md:flex-row md:items-end"><div><h1 className="section-title">The collection</h1><p className="mt-4 text-sm text-[#73736d]">Thoughtful watches for every kind of day.</p></div><div className="flex items-center border-b border-black/25 pb-2"><Search size={17} className="mr-3 text-[#9a9a93]"/><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search watches" className="w-52 bg-transparent text-sm outline-none placeholder:text-[#aaa9a3]"/></div></div><div className="flex flex-col gap-8 py-8 lg:flex-row"><aside className="w-full shrink-0 lg:w-52"><div className="mb-4 flex items-center gap-2 text-xs font-bold uppercase tracking-widest"><SlidersHorizontal size={15}/> Filter</div><select value={category} onChange={(e) => setCategory(e.target.value)} className="w-full border border-black/15 bg-transparent px-3 py-3 text-xs outline-none"><option>All</option>{["Men's","Women's","Smart","Luxury","Sports","Couple"].map((item) => <option key={item}>{item}</option>)}</select></aside><div className="flex-1"><div className="mb-7 flex items-center justify-between text-xs text-[#888780]"><span>{filtered.length} pieces</span><select value={sort} onChange={(e) => setSort(e.target.value)} className="bg-transparent text-xs font-bold uppercase tracking-wider text-[#111] outline-none"><option>Featured</option><option>Newest</option><option>Price low to high</option><option>Price high to low</option></select></div><div className="grid grid-cols-2 gap-x-3 gap-y-12 md:grid-cols-3 lg:gap-x-5">{filtered.map((product) => <ProductCard key={product.id} product={product}/>)}</div></div></div></div></main> }
