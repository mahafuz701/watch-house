{
  "name": "tuhin-watch-house",
  "version": "1.0.0",
  "private": true,
  "description": "Tuhin Watch House — production-ready E-commerce platform (Next.js + Express + Prisma)",
  "workspaces": ["backend", "frontend"],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "db:setup": "npm run setup -w backend",
    "db:seed": "npm run seed -w backend",
    "build": "npm run build -w backend && npm run build -w frontend",
    "start": "npm run start -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}